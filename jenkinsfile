pipeline {
    agent any
    stages {
        stage("Clone the project") {
            steps{
                    // Cloner le projet depuis le référentiel Git en utilisant les informations fournies
                git branch: 'main', credentialsId: 'ssh-master' , url: 'git@github.com:nidhal19615/CrudJenkins.git', 
                }
            }
        
        // Vous pouvez ajouter d'autres étapes ici
        }
  
        }